"""
Importing a package (directory with __init__.py).

A directory becomes a package when it contains an __init__.py file.
This is the difference between a module (single .py file) and a package (directory).
"""

import mypackage

mypackage.say_hello()
